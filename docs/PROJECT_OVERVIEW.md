# Северная Корзина - Платформа Коллективных Закупок

## 📋 Описание проекта

**"Северная Корзина"** - это веб-приложение для организации коллективных закупок товаров в отдаленных регионах. Система позволяет пользователям объединяться для совместного заказа товаров, что значительно снижает стоимость доставки и делает покупки более выгодными.

### 🎯 Основная идея:
- Пользователи добавляют товары в корзину и оплачивают заказы
- Заказы автоматически объединяются в "партии" (коллективные закупки)
- Когда набирается достаточное количество заказов или нужная сумма - отправляется общая машина
- Все участники получают свои товары по значительно более низкой цене доставки

## 🏆 Статус проекта: **ГОТОВ К ПРОДАКШЕНУ**

### ✅ Полностью реализовано:
- **Авторизация через SMS** - полный цикл регистрации и входа
- **Каталог товаров** - просмотр, поиск, добавление в корзину  
- **Корзина и оформление заказов** - с выбором адреса доставки
- **Платежная система** - интеграция с ЮKassa + автоматические webhook'и
- **Партии (коллективные закупки)** - автоматическое объединение заказов
- **Админская панель** - полное управление системой
- **SMS уведомления** - автоматические уведомления о статусах
- **Управление доставкой** - кнопки "Машина уехала/приехала"

## 🛠️ Техническая архитектура

### Frontend (Flutter Web/Mobile)
```
lib/
├── screens/              # Экраны приложения
│   ├── auth/            # Авторизация и регистрация
│   ├── catalog/         # Каталог товаров
│   ├── cart/            # Корзина
│   ├── orders/          # Заказы пользователя
│   └── admin/           # Админская панель
├── services/            # API сервисы
│   ├── api_service.dart         # Основной API клиент
│   ├── admin_api_service.dart   # Админский API
│   └── sms_service.dart         # SMS верификация
├── providers/           # State Management (Provider)
├── models/             # Модели данных
└── constants/          # Константы (статусы, цвета)
```

### Backend (Node.js + Express + PostgreSQL)
```
src/
├── server.js                # Основной файл сервера
├── routes/                  # API маршруты
│   ├── auth.js             # Авторизация (/api/auth/*)
│   ├── users.js            # Пользователи
│   ├── products.js         # Товары
│   ├── orders.js           # Заказы
│   ├── payments.js         # Платежи + webhook'и ЮKassa
│   ├── batches.js          # Партии (коллективные закупки)
│   ├── admin.js            # Админские операции
│   └── sms.js              # SMS отправка
├── middleware/             # Промежуточное ПО
├── utils/                  # Утилиты
│   └── batchCalculations.js # Расчет статистики партий
└── prisma/                # ORM и схема БД
```

## 🔄 Жизненный цикл заказа

### 1. **Создание заказа:**
- Пользователь добавляет товары в корзину
- Оформляет заказ с выбором адреса
- Заказ создается со статусом `pending` (ожидает оплаты)
- **Автоматически привязывается к активной партии**

### 2. **Оплата:**
- Пользователь переходит к оплате через ЮKassa
- После успешной оплаты **webhook автоматически** меняет статус на `paid`
- Сумма заказа добавляется к "собранной" сумме партии

### 3. **Управление доставкой (админ):**
- **"Машина уехала"**: все `paid` заказы → `shipped` + SMS пользователям
- **"Машина приехала"**: все `shipped` заказы → `delivered` + SMS пользователям

### 4. **SMS уведомления:**
- Умная отправка: один номер = одно SMS (независимо от количества заказов)
- Автоматические уведомления при каждой смене статуса

## 💳 Интеграция с платежами (ЮKassa)

### Полный цикл оплаты:
1. **Создание платежа** - Frontend → Backend → ЮKassa API
2. **Переадресация** - Пользователь → ЮKassa (форма оплаты)  
3. **Webhook** - ЮKassa → Backend (`https://api.sevkorzina.ru/api/payments/webhook`)
4. **Автообновление** - Статус заказа `pending → paid` + обновление статистики партии

### Текущий статус:
- **Тестовый режим**: Полностью настроен и работает ✅
- **HTTPS**: Настроен для webhook'ов ✅
- **Готовность к продакшену**: Нужно только переключить на боевой режим

## 🎛️ Админская панель

### Основные функции:
- **Управление пользователями** - просмотр, блокировка
- **Управление товарами** - добавление, редактирование, категории
- **Управление заказами** - просмотр, фильтрация по статусам
- **Управление партиями**:
  - Просмотр всех партий с детальной статистикой
  - **Правильные суммы** - "Собрано" показывает только оплаченные заказы
  - Кнопки управления доставкой:
    - **"Машина уехала"** - массовый перевод `paid → shipped`
    - **"Машина приехала"** - массовый перевод `shipped → delivered`
  - Автоматические SMS уведомления пользователям

## 📱 SMS Система

### Интеграция с SMS.aero:
- **Авторизация**: SMS коды для входа в систему
- **Уведомления о статусах**: Автоматические SMS при изменении статуса заказа
- **Оптимизация**: Один номер = одно SMS (группировка по пользователям)

### Примеры сообщений:
- `"Машина уехала за вашим заказом. Ожидайте доставку."`
- `"Машина уехала за вашими заказами (3 шт). Ожидайте доставку."`
- `"Машина прибыла с вашим заказом. Можете забирать."`

## 🏗️ Инфраструктура (Yandex Cloud)

### Серверы:
- **Основной сервер**: Ubuntu 20.04 на Yandex Cloud
- **База данных**: PostgreSQL (Yandex Managed PostgreSQL)
- **Веб-сервер**: Nginx (reverse proxy)

### Домены:
- **`sevkorzina.ru`** - главная страница с описанием проекта
- **`app.sevkorzina.ru`** - веб-приложение (Flutter Web)
- **`api.sevkorzina.ru`** - API сервер с SSL для webhook'ов

### SSL сертификаты:
- **Let's Encrypt** настроен для API (обязательно для ЮKassa webhook'ов)
- **Nginx** настроен как reverse proxy HTTPS → HTTP:3000

## 📊 Ключевые метрики системы

### Производительность:
- **API**: Node.js с connection pooling
- **База данных**: Индексированные запросы
- **Frontend**: Flutter Web - быстрая загрузка

### Безопасность:
- **JWT токены** для авторизации  
- **SMS верификация** для входа
- **HTTPS** для всех критических API
- **Валидация данных** на всех уровнях

## 🚀 Готовность к запуску

### ✅ Полностью готово:
1. **Вся функциональность реализована** - от регистрации до доставки
2. **Тестирование завершено** - все ключевые функции проверены
3. **Инфраструктура настроена** - серверы, домены, SSL
4. **Интеграции работают** - ЮKassa, SMS.aero
5. **Админские инструменты** - полное управление системой

### 🔄 Для продакшена остается:
1. **ЮKassa**: переключить с тестового режима на боевой
2. **Финальное тестирование**: с реальными платежами  
3. **Мониторинг**: настроить систему мониторинга (опционально)

### 💡 Рекомендации по запуску:
1. **Начать с малого**: пригласить 10-20 тестовых пользователей
2. **Мониторить логи**: особенно webhook'и и SMS отправку
3. **Постепенное расширение**: добавлять новых пользователей по мере роста

## 📞 Техническая информация

### Основные технологии:
- **Frontend**: Flutter Web/Mobile
- **Backend**: Node.js + Express  
- **База данных**: PostgreSQL + Prisma ORM
- **State Management**: Provider (Flutter)
- **Authentication**: JWT + SMS коды
- **Payments**: ЮKassa API
- **SMS**: SMS.aero API

### Команды для запуска:
```bash
# Backend (Node.js) - системный сервис
sudo systemctl start severnaya-korzina
sudo systemctl status severnaya-korzina
sudo systemctl restart severnaya-korzina

# Просмотр логов
journalctl -u severnaya-korzina -f

# Frontend (Flutter)  
flutter run -d chrome

# Админка (Flutter)
flutter run -d chrome
```

### Переменные окружения:
```env
DATABASE_URL=postgresql://...
JWT_SECRET=...
YOOKASSA_SHOP_ID=...
YOOKASSA_SECRET_KEY=...
SMS_AERO_EMAIL=...
SMS_AERO_API_KEY=...
```

---

## 🎉 Заключение

**Проект "Северная Корзина" полностью готов к использованию.** Все ключевые функции реализованы, протестированы и работают стабильно. Система готова помочь жителям отдаленных регионов экономить на коллективных закупках товаров.

**Время запуска: ГОТОВ ПРЯМО СЕЙЧАС!** 🚀