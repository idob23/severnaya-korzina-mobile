# Северная Корзина - Платформа Коллективных Закупок

## 📋 Описание проекта

**"Северная Корзина"** - это веб-приложение для организации коллективных закупок товаров в отдаленных регионах. Система позволяет пользователям объединяться для совместного заказа товаров, что значительно снижает стоимость доставки и делает покупки более выгодными.

### 🎯 Основная идея:
- Пользователи добавляют товары в корзину и оплачивают заказы
- Заказы автоматически объединяются в "партии" (коллективные закупки)
- Когда набирается достаточное количество заказов или нужная сумма - отправляется общая машина
- Все участники получают свои товары по значительно более низкой цене доставки

## 🚨 Статус проекта: **ТРЕБУЕТ СРОЧНОГО ИСПРАВЛЕНИЯ**

### ⚠️ КРИТИЧЕСКАЯ ПРОБЛЕМА - НЕ РАБОТАЮТ ЗАКАЗЫ

После отката базы данных и приложения к предыдущей версии:

**❌ СЛОМАНО:**
- **Создание заказов в базе данных** - API возвращает ошибку 500
- **Отображение заказов пользователей** - показывает пустой список
- **Связь Frontend → Backend для заказов** - нарушена логика создания

**✅ РАБОТАЕТ:**
- **Авторизация через SMS** - полный цикл регистрации и входа
- **Каталог товаров** - просмотр, поиск, добавление в корзину  
- **Корзина и оформление** - до момента создания заказа
- **Платежная система** - ЮKassa работает, но заказы не создаются
- **Админская панель** - управление системой
- **SMS уведомления** - отправка работает

## 🎯 ПЕРВООЧЕРЕДНАЯ ЗАДАЧА

**Восстановить функционал создания и отображения заказов:**

1. **Исправить API `POST /api/orders`** - сейчас возвращает 500 ошибку
2. **Проверить метод `createOrder` в Flutter** - возможно отсутствует или поврежден
3. **Восстановить логику сохранения заказов в БД** после успешной оплаты
4. **Проверить связь Frontend ↔ Backend** для заказов

### 🔍 Симптомы проблемы:
- Пользователь может добавить товары в корзину ✅
- Пользователь может пройти к оплате ✅
- Оплата проходит успешно ✅
- **НО: Заказ НЕ создается в базе данных** ❌
- **НЕ отображается в разделе "Мои заказы"** ❌

### 📝 Логи ошибки:
```
API: POST http://84.201.149.245:3000/api/orders
API: Ответ - 500
API: Тело ответа - {"error":"Внутренняя ошибка сервера"}
```

## 🛠️ Техническая архитектура

### Frontend (Flutter Web/Mobile)
```
lib/
├── screens/              # Экраны приложения
│   ├── auth/            # Авторизация и регистрация ✅
│   ├── catalog/         # Каталог товаров ✅
│   ├── cart/            # Корзина ✅
│   ├── orders/          # Заказы пользователя ❌ СЛОМАНО
│   └── admin/           # Админская панель ✅
├── services/            # API сервисы
│   ├── api_service.dart         # Основной API клиент ❌ createOrder сломан
│   ├── admin_api_service.dart   # Админский API ✅
│   └── sms_service.dart         # SMS верификация ✅
├── providers/           # State Management (Provider)
│   ├── orders_provider.dart     # ❌ СЛОМАН - не создает заказы
├── models/             # Модели данных
└── constants/          # Константы (статусы, цвета)
```

### Backend (Node.js + Express + PostgreSQL)
```
src/
├── server.js                # Основной файл сервера ✅
├── routes/                  # API маршруты
│   ├── auth.js             # Авторизация (/api/auth/*) ✅
│   ├── users.js            # Пользователи ✅
│   ├── products.js         # Товары ✅
│   ├── orders.js           # Заказы ❌ POST /orders возвращает 500
│   ├── payments.js         # Платежи + webhook'и ЮKassa ✅
│   ├── batches.js          # Партии (коллективные закупки) ✅
│   ├── admin.js            # Админские операции ✅
│   └── sms.js              # SMS отправка ✅
├── middleware/             # Промежуточное ПО
├── utils/                  # Утилиты
│   └── batchCalculations.js # Расчет статистики партий
└── prisma/                # ORM и схема БД
```

## 🔄 Что ДОЛЖНО работать (но сломано)

### 1. **Создание заказа:**
- Пользователь добавляет товары в корзину ✅
- Оформляет заказ с выбором адреса ✅
- ❌ **Заказ НЕ создается в БД** - API возвращает 500
- ❌ **Автопривязка к партии не работает**

### 2. **Оплата:**
- Пользователь переходит к оплате через ЮKassa ✅
- ❌ **Заказа нет в системе**, поэтому webhook не может обновить статус
- ❌ **Статистика партий не обновляется**

### 3. **Отображение заказов:**
- ❌ **Раздел "Мои заказы" показывает пустой список**
- ❌ **API `GET /api/orders` возвращает `[]`**

## 🏗️ Инфраструктура (Yandex Cloud)

### Серверы:
- **Основной сервер**: Ubuntu 20.04 на Yandex Cloud ✅
- **База данных**: PostgreSQL (Yandex Managed PostgreSQL) ✅
- **Веб-сервер**: Nginx (reverse proxy) ✅

### Домены:
- **`sevkorzina.ru`** - главная страница с описанием проекта ✅
- **`app.sevkorzina.ru`** - веб-приложение (Flutter Web) ✅
- **`api.sevkorzina.ru`** - API сервер с SSL для webhook'ов ✅

### SSL сертификаты:
- **Let's Encrypt** настроен для API ✅
- **Nginx** настроен как reverse proxy HTTPS → HTTP:3000 ✅

## 📊 Состояние системы

### ✅ Работает:
- **Авторизация**: SMS коды, JWT токены
- **Каталог товаров**: просмотр, поиск
- **Корзина**: добавление/удаление товаров
- **Платежная система**: ЮKassa интеграция
- **Админка**: управление пользователями, товарами
- **SMS**: уведомления работают
- **Инфраструктура**: сервера, домены, SSL

### ❌ Сломано:
- **Создание заказов**: API POST /orders → 500
- **Отображение заказов**: пустой список 
- **Привязка к партиям**: заказы не попадают в закупки
- **Webhook'и**: не обновляют статусы (т.к. заказов нет)

## 🚀 План восстановления

### Шаг 1: Диагностика
- [ ] Проверить логи сервера при создании заказа
- [ ] Найти точную ошибку в `src/routes/orders.js`
- [ ] Проверить наличие метода `createOrder` в Flutter

### Шаг 2: Исправление Backend
- [ ] Исправить ошибку в API `POST /api/orders`
- [ ] Проверить валидацию входных данных
- [ ] Убедиться в корректной работе с БД

### Шаг 3: Исправление Frontend
- [ ] Восстановить метод `createOrder` в `api_service.dart`
- [ ] Проверить `orders_provider.dart`
- [ ] Убедиться в правильной передаче данных

### Шаг 4: Тестирование
- [ ] Создать тестовый заказ
- [ ] Проверить сохранение в БД
- [ ] Проверить отображение в списке заказов
- [ ] Проверить обновление через webhook'и

## 💡 Возможные причины поломки

1. **Git откат затронул не все файлы** - часть изменений пропала
2. **Проблема с синхронизацией БД и кода** - схема БД не соответствует коду
3. **Поврежденные зависимости** - после отката нужно переустановить packages
4. **Проблема с middleware** - сломана авторизация для API заказов

## 📞 Техническая информация

### Команды для диагностики:
```bash
# Проверить логи сервера
journalctl -u severnaya-korzina -f

# Перезапустить сервер
sudo systemctl restart severnaya-korzina

# Проверить статус
sudo systemctl status severnaya-korzina
```

### Тестовый запрос для диагностики:
```bash
curl -X POST https://api.sevkorzina.ru/api/orders \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer ТОКЕН" \
  -d '{"addressId": 1, "items": [{"productId": 1, "quantity": 1, "price": 100}]}'
```

---

## ⚠️ КРИТИЧЕСКИЙ СТАТУС

**Проект НЕ ГОТОВ к продакшену до исправления функционала заказов!**

Пользователи могут оплачивать заказы, но заказы не создаются в системе - это критическая ошибка, которая может привести к потере денег и товаров.

**ПЕРВООЧЕРЕДНАЯ ЗАДАЧА: Восстановить создание и отображение заказов!** 🚨